<header class="header">
    <div class="container">
        <div class="mr-auto">
            <a href="{{ url('/' . App::getLocale()) }}" class="logo">
                <img src="{{ __('layouts.mossebo_market_logo') }}"
                     alt="{{ config('app.name', 'Laravel') }}"
                     class="logo__image">
            </a>
        </div>
    </div>
</header>
